# Workspace Rules for GraphLock Project

- After making any code changes or modifications, always ask the user if they want to push the changes to GitHub.
- The remote repository is already configured as `git@github.com:c0pperfi3ld/graphlock-authentication-system.git`.
